# Keep the shared narration environment compatible across F5-TTS, wandb, and
# the other Python services that share the same virtual environment.
#
# F5-TTS currently imports `wandb` even on inference code paths. The currently
# compatible wandb builds require protobuf major versions below 7.
protobuf>=4.25.8,<7
# setuptools>=81 removed the vendored pkg_resources. resemble-perth==1.0.1 (a Chatterbox dep) imports
# pkg_resources, so without this pin perth's watermarker loads as None and
# ChatterboxMultilingualTTS.from_pretrained() crashes at runtime with "'NoneType' object is not callable".
setuptools<81
