/**
 * Re-export the ModelManagementTab component from the modelManagement folder
 * This maintains backward compatibility with existing imports
 */
import ModelManagementTab from './modelManagement/ModelManagementTab';

export default ModelManagementTab;
